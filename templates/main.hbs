<!DOCTYPE html>
<html lang="pt">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>{{titulo}}</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f7f7f7;
      color: #424242;
    }
    .container {
      max-width: 600px;
      margin: 30px auto;
      background-color: #fff;
      border-radius: 10px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
      overflow: hidden;
    }
    .header {
      background: linear-gradient(135deg, #1a237e, #0d47a1);
      padding: 30px 20px;
      color: #ffffff;
      text-align: center;
    }
    .header h1 {
      margin: 0;
      font-size: 24px;
    }
    .content {
      padding: 35px 30px;
    }
    .greeting {
      font-size: 20px;
      font-weight: 600;
      margin-bottom: 20px;
      color: #1a237e;
    }
    .message {
      font-size: 16px;
      margin-bottom: 20px;
    }
    .details {
      background-color: #f1f3f5;
      padding: 20px;
      border-left: 4px solid #1a237e;
      border-radius: 8px;
      margin-bottom: 25px;
    }
    .button {
      display: inline-block;
      padding: 12px 25px;
      background-color: #1a237e;
      color: #ffffff;
      text-decoration: none;
      border-radius: 25px;
      font-weight: bold;
      transition: background-color 0.3s ease;
    }
    .button:hover {
      background-color: #0d47a1;
    }
    .footer {
      background-color: #f5f5f5;
      padding: 25px 20px;
      text-align: center;
      font-size: 14px;
      border-top: 1px solid #e0e0e0;
    }
    .contact-info p {
      margin: 5px 0;
    }
    .social-links a {
      margin: 0 8px;
      color: #1a237e;
      text-decoration: none;
      font-weight: 500;
    }
    .social-links a:hover {
      text-decoration: underline;
    }
    .copyright {
      margin-top: 15px;
      color: #9e9e9e;
      font-size: 12px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>{{empresa}}</h1>
    </div>

    <div class="content">
      <div class="greeting">{{saudacao}} {{nome}},</div>
      <div class="message">{{mensagemPrincipal}}</div>
      <div class="message">{{mensagemFinal}}</div>

      {{#if botaoTexto}}
      <div style="text-align: center;">
        <a href="{{botaoLink}}" class="button">{{botaoTexto}}</a>
      </div>
      {{/if}}
    </div>

    <div class="footer">
      <div class="contact-info">
        <p><strong>{{empresa}}</strong></p>
        <p>{{slogan}}</p>
        <p>{{endereco}}</p>
        <p><strong>Email:</strong> {{email}}</p>
        <p><strong>Tel:</strong> {{telefone}}</p>
      </div>

      <div class="social-links">
        {{#each redesSociais}}
          <a href="{{this.url}}" target="_blank">{{this.nome}}</a>
        {{/each}}
      </div>

      <div class="copyright">
        &copy; {{ano}} {{empresa}}. Todos os direitos reservados.
      </div>
    </div>
  </div>
</body>
</html>
